# 部署记录与问题排查 (DEPLOYMENT_LOG)

## 1. 结论
本文档专录历届发布周期的关键部署或打包操作备忘事宜、预见问题及其排查思路。

## 2. 范围/假设
适用于本应用 v1 发行版在跨入真实应用场景及执行打包 (如使用 `pyinstaller` 到异构 Windows 等环境里) 可能滋生的日志排查。

## 3. 设计要点
- 定期整理归类由运行、编译及网络连通性上所遇到的高频疑点排查路线。

## 4. 施工记录
### V1.0.0 初始化记录与排查建议
- **打包兼容性提示**：如果是生成跨平台 `exe` 遇到闪退情况，多表现为系统缺失必要的运行时库或在无控制台（`-w` 参数下）屏蔽了错误日志。排查时建议：**将单体 `-w -F` 去除 `-w` 重打包并在 CMD 打开查看 `ImportError`，再以此使用 `pip` 或 `hiddenimports` 追加**。
- **表格抽取特记**：目前我们按“国际专利分类”及“关键词”对列名称进行弱匹配容错。当用户导入极不规范非闭合线条之表单导致 `get_tables()` 一无所获时，可从代码预留的后备猜估列位逻辑进行干预定制。
- **接口连通性**：若 DeepSeek 返回 `Connection Error`，说明网络被阻断或无法与 `api.deepseek.com` 取得握手（请使用全局网络）。

## 5. 验收标准
1. 日志格式标准化，故障描述详实可供追溯。

## 6. 风险与回滚
由于单纯起到参考维库的作用，内容不会直接拖累主功能业务逻辑；若出现排查对策过时现象按更新节点废除老旧词条即可。
