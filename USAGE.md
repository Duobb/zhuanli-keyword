# 使用手册 (USAGE)

## 1. 结论
本文档指导您如何进行从文档导入到最终获得 AI 关键词智能匹配结果的全部端到端操作。

## 2. 范围/假设
假设用户持有已打包好的 `exe` 或者可直接运行源码环境，并拥有有效的 DeepSeek API 密钥。

## 3. 设计要点
- 流程化分为“文档加载区”、“规则搜索提取区”、“大语言模型整合分析区”三个使用闭环。

## 4. 施工步骤 (核心使用流)

### 步骤 A：载入参考文档知识库
1. 工具左侧区域点击 **添加文档 (PDF)**。
2. 选取目标带有分类号的 PDF 专利文档，确认后系统会驻留解析数据，完成后即可进行检索。

### 步骤 B：执行星号递减搜索与匹配
1. 在“目标分类号”输入源字串 (例如：`G06F3/01`)。
2. 点击 **搜索** 按钮。
3. 界面右上方文本框会显示提取出的关键词串，工具将自动剥离分隔符并执行 `G06F3/01` -> `G06F3*` -> `G06F*` 等机制，保障结果的鲁棒性。

### 步骤 C：DeepSeek 上下文指引分析
1. 界面顶端，填设获取得到的 **DeepSeek API Key**。
2. 于界面右下方“待分析文字”编辑框中贴入您需要比对匹配的核心长文本。
3. 点击 **开始 AI 分析**，此时引擎将自动结合【步骤 B】产出的有效关键词，投喂给模型获取最终解读报告。
4. 解读报告展于下方只读展示区。

## 5. 验收标准
1. 提取操作能在任意包含目标关键字的文档中响应出正确的上下文。
2. AI 分析回显完整、断网或密钥错误时提供直观弹窗警告提示。

## 6. 风险与回滚
- **风险**：DeepSeek 接口若有临时封堵现象会导致无响应。
- **回滚**：请确保密钥有效或稍后重新点击分析重试。
