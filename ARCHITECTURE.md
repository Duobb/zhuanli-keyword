# 架构与配置文档 (ARCHITECTURE)

## 1. 结论
本应用采用轻量级的自研 MVC 结构变体，基于 Python + PyQt6 实现用户交互，分离了文件解析、搜索业务逻辑与网络服务调用层，极大程度提升了打包体积可控性与代码可维护性。

## 2. 范围/假设
假设环境运行内存充足，且使用者具备基本的 Python 原理与文件操作常识。

## 3. 设计要点
- **界面与多线程交互 (`main.py`)**：GUI 包含“文档管理容器”、“搜索交互容器”、“智能分析容器”。为了保证界面在执行密集型 PDF 抽取或等待网络回包时不卡死，全盘引入 `QThread` 注入任务并借助 `pyqtSignal` 机制跨线程传递结果。
- **解析核心模块 (`document_parser.py`)**：将 `pdfplumber` 针对非规则矩形或跨页表格数据进行统一清洗（回车换行剔除机制），并针对含有“国际专利分类”与“关键词”的中英文变种列首进行自适应绑定。
- **智能搜索引流模块 (`search_engine.py`)**：独树一帜的自动后退补偿匹配算法，遇到如 `G06F3/01` 输入时依次退化并衍生出 `G06F3*`、`G06F*`、`G06*` 等直至产生最小化容错命中匹配。
- **大模型会话层 (`deepseek_api.py`)**：封装标准的 `openai` 调用客户端基架环境，固定模型型号为 `deepseek-chat`，向底层下发具有清晰指示意义的系统 `prompt` 分析任务。

## 4. 施工步骤 (文件指引)
```text
docs/
├── README.md             # 顶层索引
├── ARCHITECTURE.md       # 本文档
├── DEPLOYMENT.md         # 打包指导
├── USAGE.md              # 终端用户手册
└── DEPLOYMENT_LOG.md     # 记录可能出现的错误日志及打包心得
keyword/
├── main.py               # 界面主入口及进程调度
├── document_parser.py    # 文档表格抽取
├── search_engine.py      # 退化匹配算法检索
├── deepseek_api.py       # 大语言模型通讯实现
└── requirements.txt      # 包依赖快照列表
```

## 5. 验收标准
1. 代码结构严格遵循 MVC 或相关业务分层隔离标准，文件边界清晰无冗余跨模块高耦。
2. 架构稳健，`PyQt6` 界面始终保证丝滑过渡且不闪退卡死。

## 6. 风险与回滚
- **风险**：解析大型复杂千页级 PDF 时可能消耗大量瞬时内存。
- **回滚/升级对策**：后期可使用流解析组件如 `PyMuPDF (fitz)` 替代当前内存密集型的 `pdfplumber` 进行迭代升级。
